/// @description Reset sight surface

//if (!surface_exists(sight_surface) or !surface_exists(fog_surface)) {
//	var w = 1, l = 1;
//	if (instance_exists(terrain_control)) {
//		w = terrain_control.terrain.width*GRID_SCALE;
//		l = terrain_control.terrain.length*GRID_SCALE;
//	}
//	if (!surface_exists(sight_surface)) sight_surface = surface_create(w,l);
//	if (!surface_exists(fog_surface)) fog_surface = surface_create(w,l);
//}
//surface_set_target(sight_surface);
//draw_clear_alpha(c_black, .5);
//surface_reset_target();

